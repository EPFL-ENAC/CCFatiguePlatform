import colors from "vuetify/lib/util/colors";

export const colorPalette = [
  colors.red,
  colors.blue,
  colors.green,
  colors.amber,
  colors.deepPurple,
  colors.orange,
].map((color) => color.base);
